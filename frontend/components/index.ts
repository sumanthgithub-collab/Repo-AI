/**
 * Component stubs — all global components.
 * Each component is documented and stub-implemented.
 * Full implementations done in Phase 2 (Week 4-5).
 *
 * Components in this directory:
 *  - Navbar.tsx        → Top nav with Clerk auth state
 *  - ChatPanel.tsx     → Main chat message list + input
 *  - CodeViewer.tsx    → Monaco editor panel (right panel)
 *  - CitationChip.tsx  → Clickable file:line reference badge
 *  - QualityBadge.tsx  → RAGAS score badge (green/yellow/red)
 *  - FileTree.tsx      → Collapsible repo file tree sidebar
 *  - StreamingText.tsx → SSE token-by-token text renderer
 *  - SessionList.tsx   → Past sessions sidebar list
 *  - IngestionProgress.tsx → Live progress bar during repo ingestion
 */
